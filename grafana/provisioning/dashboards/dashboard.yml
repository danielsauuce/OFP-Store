apiVersion: 1

providers:
  - name: OFP Store
    orgId: 1
    folder: OFP
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
